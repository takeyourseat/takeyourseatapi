#--------------------------------------------------------------------------------------
#                       SQL configuration
#--------------------------------------------------------------------------------------
server.port=8083
spring.datasource.url = jdbc:mysql://mysql-oauth.default.svc.cluster.local:3306/test
spring.datasource.username = sa
spring.datasource.password = password

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = update

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager)
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect

spring.profiles.active=swagger

#------------------------------------------------------------------------
#                           Logging
#------------------------------------------------------------------------
spring.application.name=oauth-server



#logging.level.root=debug
#logging.level.org.springframework.web=info
#logging.level.com.stefanini.internship.oauthserver=info

#Logging pattern for the ocnsole
#logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

#Logging pattern for file
#logging.pattern.file==%d{yyyy-MM-ddc HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
#logging.file.name=/home/capostol/IdeaProjects/takeyourseatapi_dev/oauth-server/src/main/resources/logs/test.log

























